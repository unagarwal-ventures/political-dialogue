{
  "buildCommand": "npm install --workspace=shared && npm run build --workspace=shared && npm install --workspace=client && npm run build --workspace=client",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
